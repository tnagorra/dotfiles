# alias pdfjoin='pdfjam --fitpaper true --rotateoversize true --suffix joined'

alias day="echo 'gruvbox-light-soft' > .scheme && pkill -USR1 zsh"
alias night="echo 'gruvbox-dark-soft' > .scheme && pkill -USR1 zsh"

alias vim="nvim"
alias ls="ls --color"
